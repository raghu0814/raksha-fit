const gym=new URLSearchParams(location.search).get('gym'),form=document.querySelector('#checkinForm'),message=document.querySelector('#checkinMessage');
if(!gym){form.hidden=true;message.textContent='Invalid gym QR code.'} else {checkinInfo.textContent='Enter your mobile number to check in.';form.onsubmit=async e=>{e.preventDefault();const r=await fetch('/api/attendance/public-checkin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({gym_code:gym,phone:new FormData(form).get('phone')})});const d=await r.json();message.textContent=d.message;message.className=r.ok?'success':'error';};}
